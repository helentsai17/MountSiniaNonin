<Page
    x:Class="Mount_Sinai_Nonin_device.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mount_Sinai_Nonin_device"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Border HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
            <Button Padding="10" Margin="4,0,0,0" Background="blue" Click="Open_Menu" VerticalAlignment="Top">
                <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE700;"/>
            </Button>
        </Border>

        <SplitView Grid.Row="1" IsPaneOpen="True" x:Name="SView" DisplayMode="Inline" CompactPaneLength="28">
            <SplitView.Pane>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="28"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>
                    <SymbolIcon Margin="4" Grid.Row ="2" Symbol="Contact2" HorizontalAlignment="Left"/>
                    <SymbolIcon Margin="4" Grid.Row ="1" Symbol="RepeatAll" HorizontalAlignment="Left"/>
                    <SymbolIcon Margin="4" Grid.Row ="0" Symbol="Globe" HorizontalAlignment="Left" />
                    <Button Background="{x:Null}" Grid.Row="0" Grid.Column="1" Margin="4" Content="Find Device" FontSize="30" Click="Goto_FindDevice" />
                    <Button Background="{x:Null}" Grid.Row="1" Grid.Column="1" Margin="4" Content="Data" FontSize="30" Click="Goto_SeeData"/>
                    <Button Background="{x:Null}" Grid.Row="2" Grid.Column="1" Margin="4" Content="Person" FontSize="30" Click="Goto_UserInfo"/>
                    <Button Background="{x:Null}" Grid.Row="3" Grid.Column="1" Margin="4,0,0,0" Content="Toast" FontSize="30" Click="ToastNotification" VerticalAlignment="Bottom"/>
                </Grid>
            </SplitView.Pane>
            <Frame  x:Name="InnerFrame"/>
        </SplitView>

    </Grid>
</Page>
